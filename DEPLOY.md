# 布丁喵上线指南 🍮

三步就能让顾客手机下单、店里 POS 实时收单。跟着做即可，不需要懂代码。

---

## 第 1 步：建数据库（一次性，约 2 分钟）

1. 打开你的 Supabase 项目 → 左侧 **SQL Editor** → **New query**
2. 打开本仓库的 **`supabase-setup.sql`**，把里面**全部内容**复制粘贴进去
3. 点右下角 **Run**（跑成功会显示 Success）

这一步会自动建好 `orders`（订单）和 `menu_items`（菜单）两张表、开启实时推送、并把当前 13 个菜品灌进去。

> ⚠️ 这个脚本会**清空**已有订单再重建。只在第一次上线时跑；以后不要重复跑第 1 节，否则历史订单会没。

密钥说明：`pudding-meow.html` 和 `pos.html` 顶部已经填好了你的 `SUPABASE_URL` 和 `SUPABASE_KEY`（两个文件必须一致）。以后换项目时，两个文件都要改。

---

## 第 2 步：把网站放上线（Vercel，免费）

前提：先把本次改动的 PR 合并到 `main` 分支（在 GitHub 点 Merge）。Vercel 连着你的仓库，合并后会自动重新部署。

1. Vercel 已连本仓库，`main` 分支一有更新就自动上线。
2. 部署好后的两个网址：
   - 顾客点单：`https://pudding-meow.vercel.app/pudding-meow.html`
   - 店里 POS：`https://pudding-meow.vercel.app/pos.html`

> 两个页面同源，实时联动天然成立。想要更短的自定义域名（如 `puddingmeow.com`）可以后面再加，告诉我即可。

---

## 第 3 步：做桌面二维码

拿第 2 步顾客点单的网址，用任意免费「二维码生成」工具生成二维码，打印摆桌，顾客扫码即可点单。
（把最终网址发我，我也可以直接帮你生成一张二维码图片放进仓库。）

---

## 第 4 步（选做）：外卖地址联想 + 地图

只有**外卖**下单要用。开了之后，顾客在「新收货地址」页打字就有地址下拉联想，选一条自动定位到地图上、拿到经纬度，Lalamove 才能算配送费。不开也不影响堂食/自取——地址页会自动回退成「手打地址 + 📍使用我的定位」的老样子，照样能下单。

### 4.1 申请 Google Maps 密钥（网页操作，约 10 分钟）

1. 打开 <https://console.cloud.google.com/> → 登录 → 顶部「选择项目」→「新建项目」（名字随意，如 `pudding-meow`）。
2. **绑定结算（信用卡）**：Google Maps 每月约有 **$200 美元免费额度**，奶茶店用量根本用不完，基本等于免费；但不绑卡地图会打「仅供开发」灰色水印。
3. 左上菜单 →「API 和服务」→「库」，搜索并逐个**启用**这 3 个（缺一个对应功能就废）：
   - **Maps JavaScript API**（地图本身）
   - **Places API**（地址联想）
   - **Geocoding API**（GPS 定位反查地址）
4. 「API 和服务」→「凭据」→「+ 创建凭据」→「API 密钥」，复制那串 `AIzaSy...`。
5. **给密钥上锁（最重要，别跳过）**——密钥会出现在网页源码里谁都看得到，锁死用途后别人偷了也没用：
   - **应用限制** → 选「HTTP 引荐来源网址」，加你的域名，如
     `https://pudding-meow.vercel.app/*`（末尾用 `*`，这样带 `?ref=邀请码` 参数的链接也不会被拦；用自定义域名就填你自己的）。
   - **API 限制** → 选「限制密钥」→ 只勾上上面那 3 个 API。
   - 保存。

> 这就是为什么这把密钥能安全放前端：它只能在你的域名上、只干这三件事，跟 Lalamove / HitPay 那种必须藏在后端 Edge Function 的密钥不一样。**前提是第 5 步的上锁一定要先做好**，因为密钥一 push 到 GitHub 就等于公开了。

### 4.2 填进代码

打开 `pudding-meow.html`，把顶部（约第 2032 行）这一行的占位符换成你的密钥：

```js
var GOOGLE_MAPS_KEY = 'YOUR-GOOGLE-MAPS-API-KEY';   // ← 换成 AIzaSy... 那串
```

下面一行 `STORE_LATLNG` 是地图默认落点（现在填的是 Kota Syahbandar 大概坐标）。想更准就去 Google Maps 搜你店名、右键复制经纬度填进去；不填也行，顾客反正会自己把 📍 拖到自家门口。

改完在真实浏览器里试（这台开发机连不上 Google，测不了）：外卖模式 → 新收货地址 → 打字应出现下拉联想，选一条地图定位过去，配送费能算出来。

---

## 上线前自测清单（在真实浏览器里，2 台设备或 2 个不同浏览器）

> 关键：要用**两台设备**或**两个不同浏览器**（例如手机 + 电脑），这样才证明是「云端跨设备」而不是本机。

1. **手机**打开顾客点单页 → 选商品 → 购物车 → 选「柜台支付」→ 确认下单
2. **电脑**打开 POS → 顶部「Pending Payment / 待付款」→ 应**几秒内**看到这单，标注 `📱线上·自取`，弹出 `🔔 新线上订单`
3. POS 点该单「已付款」→ **手机**上这单状态应实时变成「制作中」
4. POS 后台 → **菜单管理** → 改某个菜的价格 / 点「下架」→ **手机**刷新点单页 → 价格更新 / 该菜消失
5. TNG 预付单：顾客选 TNG 付款的单**不会**进待付款队列（因为已经线上付了）——这是正确的

全部通过 = 正式可营业。任何一步不对，把现象告诉我。

---

## 日常使用

- **改菜单 / 改价 / 上下架 / 加新品**：POS → 登录（默认 PIN `0000`）→ 进后台 → **菜单管理**。改完顾客端自动更新。
- **收单**：顾客线上下单进「待付款」；到店/柜台付款后点「已付款」。店员在 POS 自己开的单照旧。

## 收款方式

- **柜台/现金**：顾客下单，到店付款，店员点「已付款」。
- **TNG**：顾客下单时跳转到你的 TNG 收款链接。当前用的是代码里预置的链接——要换成你自己的 TNG 商家收款码/链接，告诉我即可替换。

## 已知限制（可后续升级）

- 顾客端「订单」页目前显示全部近期订单（没有「只看自己的」——那需要加会员登录识别身份）。
- 菜单后台写权限目前对匿名开放（POS 只有本机 PIN，没有真正的店员登录）。稳定运营后建议加店员账号鉴权收紧。
- 会员积分体系仍存在本机（未上云）。
